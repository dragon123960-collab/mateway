# 连续会话复用-本地-2

## 问题
继续上一轮，把刚才的总结压缩成三条，并说明下一步该优先检查什么。

## 结果
## Mateway 测试目标压缩

### 三条关键点

1. **运行机制连通性**：验证命令入口、runtime 主链、session/task 绑定、本地文件读取和 trace 记录是否完整
2. **真实模型调用**：确保不走假数据，能生成可定位失败的 Markdown 报告
3. **会话状态复用**：验证连续任务能否在同一 session 下正确接续和加载历史

### 下一步应优先检查

**runtime 主链事件是否完整**：

- `runtime.receive` → `runtime.plan` → `runtime.tool_start` → `runtime.tool_done` → `runtime.reply` 是否全部触发
- 是否卡在 `await_confirm` 或 `await_user_input`
- session 是否按预期持久化

## 结论
任务已完成。

## 执行过程与参数
- trace_id: batch-session-20260520-local
- session_key: batch-session-20260520-local
- channel: cli
- user_id: local
- thread_id: batch-session-20260520-local
- home: /Users/dongping/.mateway
- project_root: /Users/dongping/project/mateway
- generated_at: 2026-05-20T13:00:09+08:00
- trace_file: /Users/dongping/.mateway/trace/events-2026-05-20.jsonl

### Plan
```json
{
  "summary": "查找并压缩mateway测试目标总结",
  "steps": [
    {
      "id": "step-1",
      "goal": "读取测试文档获取测试目标总结",
      "tool": "file.read",
      "args": {
        "path": "/Users/dongping/project/mateway/docs/测试文档.md"
      },
      "risk": "safe_read",
      "requires_confirm": false,
      "expected_evidence": [
        "测试文档内容"
      ]
    },
    {
      "id": "step-2",
      "goal": "读取总规划文档补充上下文",
      "tool": "file.read",
      "args": {
        "path": "/Users/dongping/project/mateway/docs/总规划.md"
      },
      "risk": "safe_read",
      "requires_confirm": false,
      "expected_evidence": [
        "总规划文档内容"
      ]
    },
    {
      "id": "step-3",
      "goal": "基于文档内容压缩成三条关键点",
      "tool": "shell.run",
      "args": {
        "command": "echo '综合分析测试目标和总规划文档'",
        "workdir": "/Users/dongping/project/mateway"
      },
      "risk": "safe_read",
      "requires_confirm": false,
      "expected_evidence": [
        "整理后的三条关键点"
      ]
    }
  ]
}
```

### Tool Results
```json
[
  {
    "step_id": "step-1",
    "tool": "file.read",
    "ok": true,
    "output": "# Mateway 测试文档\n\n更新时间：2026-05-20\n\n## 目的\n\n这份文档用于记录 Mateway 的真实模型回归测试流程，并把每个测试任务的设计、执行方式和判定标准固定下来。\n\n目标不是做单元测试，而是验证整套运行机制是否连通：\n\n- 命令入口是否正常\n- runtime 主链是否正常\n- session/task 绑定是否正常\n- 本地文件读取、总结和 trace 记录是否正常\n- 连续任务是否能复用同一 session\n- 最终是否能在 `testdata/YYYY-MM-DD/` 下生成一任务一文档的结果\n\n## 执行方式\n\n统一使用：\n\n```bash\ngo run ./cmd/mateway test --title \"任务名\" --question \"任务描述\"\n```\n\n建议在需要验证连续会话时，显式指定相同的 `--session-key`。\n\n默认输出位置：\n\n- `./testdata/YYYY-MM-DD/`\n\n每个任务都会生成一份 Markdown 报告，报告标题直接使用任务名。\n\n## 判定标准\n\n每个任务至少要满足以下条件：\n\n1. 能调用真实模型，不走假数据。\n2. 能完成 runtime 主链或在合理边界内停在待确认/待补问状态。\n3. 能生成清晰的 Markdown 报告。\n4. 报告中能看出问题、结果、结论、执行过程和参数。\n5. 如果任务失败，报告中要能定位失败发生在哪一步。\n\n## 任务列表\n\n### 任务 1：基础运行体检\n\n- 任务名：`基础运行体检`\n- 目的：验证最基本的模型调用、计划生成和最终回复是否正常。\n- 问题描述：\n  - 请检查当前运行环境是否正常，说明你看到的配置、模型和可用工具情况，并给出一句结论。\n- 关注点：\n  - `doctor` 所反映的配置是否能被 runtime 正常继承\n  - 模型是否能输出可读结论\n  - Trace 是否完整\n\n### 任务 2：本地文档读取\n\n- 任务名：`本地文档读取`\n- 目的：验证 `file.read` 和项目理解路径是否正常。\n- 问题描述：\n  - 请阅读 `README.md` 和 `docs/README.md`，概括 Mateway 当前的核心架构、命令入口和测试入口。\n- 关注点：\n  - 文件读取是否受路径边界保护\n  - 模型能否正确总结本地文档\n  - 是否能把测试入口也纳入总结\n\n### 任务 3：项目风险识别\n\n- 任务名：`项目风险识别`\n- 目的：验证模型能否在现有代码和文档基础上提出合理风险点。\n- 问题描述：\n  - 请根据当前仓库的文档和运行机制，列出最值得优先验证的三个风险点，并说明每个风险点的依据。\n- 关注点：\n  - 模型是否能把 runtime、session、trace、feishu 等边界理解正确\n  - 结果是否有可执行的风险判断\n\n### 任务 4：连续会话复用\n\n- 任务名：`连续会话复用-1`\n- 目的：验证同一 session key 下的任务状态能否被复用。\n- 问题描述：\n  - 先完成一个短任务：用一句话总结当前系统最重要的测试目标。\n- 关注点：\n  - 生成的 session state 是否可被下一轮接续\n  - task 记录是否正确写入\n\n- 任务名：`连续会话复用-2`\n- 目的：验证 followup / session 绑定是否能接上上一轮。\n- 问题描述：\n  - 继续上一轮，把刚才的总结压缩成三条，并说明下一步该优先检查什么。\n- 关注点：\n  - 相同 `--session-key` 是否能触发上下文延续\n  - 是否能看出历史任务被正确加载\n\n## 结果归档\n\n执行完成后，每个任务都应在以下目录留下独立文档：\n\n```text\ntestdata/YYYY-MM-DD/\n```\n\n文件名建议包含时间戳和任务名，方便按任务快速定位。\n\n## 建议检查项\n\n完成一批任务后，优先检查：\n\n- 是否有任何任务卡在 `await_confirm` 或 `await_user_input`\n- `runtime.receive / runtime.plan / runtime.tool_start / runtime.tool_done / runtime.reply` 是否都出现\n- session 是否按预期保存\n- 报告中的 trace 事件是否和最终结论一致",
    "evidence": {
      "bytes": 3844,
      "kind": "file_read",
      "path": "/Users/dongping/project/mateway/docs/测试文档.md"
    }
  },
  {
    "step_id": "step-2",
    "tool": "file.read",
    "ok": true,
    "output": "下面是更新后的完整规划文档版本。我把**多 Agent 共存、spawn/subagent、DAG、PlanExecute 的取舍**也合进去了，并补上了 Pi Agent / OpenClaw 的参考依据和链接。\n\n---\n\n# 最小 Agent 内核架构规划文档\n\n## Pi-like Runtime + Skills + Tools + 多 Agent 共存路线\n\n## 1. 当前结论\n\n你现在的系统应该走这条路线：\n\n```text\n最小 Agent Runtime\n  + 内置 FollowupResolver\n  + 内置 SessionState / TaskState\n  + 内置 SkillDiscovery\n  + 稳定 ToolRegistry / ToolExecutor\n  + AgentLoop 工具调用闭环\n  + ResponseSanitizer\n  + 外置 Skills 扩展具体能力\n  + 后续支持多个 Agent 共存\n```\n\n当前不建议优先做：\n\n```text\n复杂 Router\n复杂 DAG 主流程\n默认 PlanExecute\n复杂 FinalVerifier\nspawn/subagent\n多 Agent Supervisor\n```\n\n核心判断：\n\n```text\n内核只负责“运行机制”；\n具体智能能力沉淀为 Skills；\n多个长期 Agent 由 Gateway 路由共存；\nspawn/subagent 等单 Agent 稳定后再做。\n```\n\n---\n\n# 2. Pi Agent / OpenClaw 的借鉴点\n\nPi 的定位是一个 **minimal terminal coding harness**，它强调用 Extensions、Skills、Prompt Templates、Themes 来扩展工作流，而不是把所有能力塞进核心。Pi 官方 README 里也明确提到，它跳过了 sub agents 和 plan mode，主张让用户通过扩展包按需构建自己的工作流。([GitHub][1])\n\nOpenClaw 的 Pi 集成思路也很值得借鉴。OpenClaw 文档说明，它不是把 Pi 当作外部子进程或 RPC 服务，而是通过 `createAgentSession()` 嵌入 Pi 的 `AgentSession`，从而控制 session 生命周期、事件处理、工具注入、系统 prompt、session 持久化等。([OpenClaw][2])\n\nOpenClaw 的多 Agent 也不是 Pi 内核自带的 subagent，而是 Gateway 层托管多个隔离 Agent：每个 agent 有自己的 workspace、`SOUL.md`、`AGENTS.md`、可选 `USER.md`、独立 `agentDir` 和 session store。([OpenClaw][3])\n\n所以可以借鉴的核心是：\n\n```text\nPi-like 小核心：\n  AgentLoop + Tools + Session + Skills\n\nOpenClaw-like 多 Agent：\n  Gateway 路由多个隔离 Agent Profile\n```\n\n而不是：\n\n```text\n一开始就做复杂多 Agent 编排、DAG、PlanExecute、子 Agent 调度。\n```\n\n---\n\n# 3. 你的系统最终定位\n\n建议把系统定位为：\n\n```text\nMateway = Minimal Agent Runtime + Skill System + Tool System + Multi-Agent Profiles\n```\n\n其中：\n\n```text\nRuntime 负责：\n- 配置\n- 通道\n- 网关\n- RuntimeContext\n- SessionState\n- FollowupResolver\n- SkillDiscovery\n- ToolRegistry\n- AgentLoop\n- ResponseSanitizer\n- Trace/Event\n\nSkills 负责：\n- fresh-search\n- source-evaluation\n- chinese-summary\n- project-review\n- code-edit\n- doc-writing\n- log-analysis\n- future planning-mode\n\nGateway 负责：\n- 多 Agent 路由\n- channel/account/peer 到 agentId 的绑定\n```\n\n这里的关键边界应该再明确一次：\n\n```text\nRuntime 内置的是 SkillDiscovery / SkillRegistry 机制；\nSkill 内容本身默认放在 workspace / a\n...[truncated]...\niew/\n\n  coding/\n    soul.md\n    agent.md\n    memory.md\n    skills/\n\n  research/\n    soul.md\n    agent.md\n    memory.md\n    skills/\n```\n\n---\n\n# 21. 开发优先级\n\n## P0：先把单 Agent 内核做稳\n\n```text\n1. RuntimeContext\n2. SessionState / TaskState\n3. 内置 FollowupResolver\n4. SkillRegistry / SkillDiscovery\n5. ToolRegistry / ToolExecutor\n6. AgentLoop 工具调用闭环\n7. ResponseSanitizer\n8. Trace/Event\n```\n\n## P1：补齐基础体验和多 Agent 共存\n\n```text\n1. fresh-search skill\n2. source-evaluation skill\n3. chinese-summary skill\n4. project-review skill\n5. project.index tool\n6. file.summary tool\n7. AgentProfile\n8. AgentRegistry\n9. AgentBinding / Gateway routing\n10. per-agent session store\n```\n\n## P2：增强协作能力\n\n```text\n1. agent.send_message\n2. agent.read_reply\n3. 已存在 Agent 之间任务转发\n4. 更细的 per-agent tool/skill allowlist\n5. per-agent model config\n```\n\n## P3：可选复杂任务能力\n\n```text\n1. planning-mode skill\n2. code-edit skill\n3.局部 DAG workflow\n4. file patch confirm flow\n5. search/filter/summarize flow\n```\n\n## P4：spawn/subagent\n\n```text\n1. session.spawn\n2. session.join\n3. child session result summary\n4. child tool/skill allowlist\n5. concurrency limit\n6. cost limit\n7. parent-child trace\n```\n\n---\n\n# 22. 现在还差什么\n\n从你当前“最小版”来看，按我们讨论，最关键还差这些：\n\n## P0 缺口\n\n```text\n1. FollowupResolver 内置化\n2. SessionState / TaskState 稳定保存\n3. AgentLoop 工具调用闭环\n4. ToolCall 不泄漏给用户\n5. ResponseSanitizer\n6. SkillDiscovery 内置\n7. PromptBuilder 注入 selected skills\n8. RuntimeContext 注入 current_date / timezone / locale\n```\n\n## P1 缺口\n\n```text\n1. fresh-search skill\n2. source-evaluation skill\n3. chinese-summary skill\n4. project-review skill\n5. project.index 工具\n6. file.summary 工具\n7. AgentProfile / AgentRegistry\n8. Gateway AgentBinding\n```\n\n## 暂时不补的缺口\n\n```text\n1. DAG 主流程\n2. PlanExecute 默认模式\n3. spawn/subagent\n4. Supervisor\n5. 复杂 FinalVerifier\n6. heartbeat 自动任务\n```\n\n---\n\n# 23. 最终决策\n\n当前阶段最终策略：\n\n```text\n主执行：\n  AgentLoop\n\n上下文连续：\n  内置 FollowupResolver + SessionState\n\n能力扩展：\n  Skills\n\n工具执行：\n  ToolRegistry + ToolExecutor\n\n项目理解：\n  project-review skill + project.index + file.summary\n\n搜索总结：\n  fresh-search + source-evaluation + chinese-summary\n\n多 Agent：\n  P1 做长期 Agent 共存\n\nspawn/subagent：\n  P3/P4 再做\n```\n\n一句话：\n\n```text\n先做 Pi-like 小内核；\n再做 OpenClaw-like 多 Agent 共存；\n最后才考虑 spawn/subagent、DAG、PlanExecute。\n```\n\n[1]: https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/README.md?utm_source=chatgpt.com \"pi/packages/coding-agent/README.md at main\"\n[2]: https://docs.openclaw.ai/concepts/agent?utm_source=chatgpt.com \"Agent runtime\"\n[3]: https://docs.openclaw.ai/concepts/multi-agent?utm_source=chatgpt.com \"Multi-agent routing\"",
    "evidence": {
      "bytes": 25969,
      "kind": "file_read",
      "path": "/Users/dongping/project/mateway/docs/总规划.md"
    }
  },
  {
    "step_id": "step-3",
    "tool": "shell.run",
    "ok": true,
    "output": "综合分析测试目标和总规划文档",
    "evidence": {
      "command": "echo '综合分析测试目标和总规划文档'",
      "exit_code": 0,
      "kind": "shell",
      "workdir": "/Users/dongping/project/mateway"
    }
  }
]
```
