# Mateway 当前进度

更新时间：2026-05-20

## 今日已完成

- 已将 `issue.md` 收敛到只保留真正未闭环的问题，删除了已验证通过或已转入进度的重复条目
- 已把飞书 followup 的 `await_confirm` 干扰场景记录进测试文档，便于后续继续回归
- 已处理 `issue.md` 第 5 项：
  - `await_confirm` 且存在 pending approval 时，明显独立的新请求会直接开启新 task
  - “同意 / 确认 / 取消”等回复仍优先作为待确认任务的批准或拒绝
  - 已补单测覆盖“待确认删除任务存在时，用户改问两句话总结测试目标”的场景
- 已处理飞书 MiniMax tool_call 标签回显：
  - runtime sanitizer 会清理 `<minimax:tool_call>` 工具调用块
  - 飞书出站渲染会二次过滤 MiniMax tool_call 标签和 `file.read args / risk / requires_confirm` 详情行
  - 已补 runtime 与 feishu 两侧单测覆盖截图里的连续 `file.read` 泄漏场景
- 已处理“测试目标总结缺少真实文档证据”的泛化回答问题：
  - 当前项目、仓库、测试目标等总结类请求必须具备 `file.read / file.summary / project.index` 证据
  - 第一版 plan 未取证时会进入 plan repair，repair 后仍无证据则阻断最终泛化总结
  - 已补单测覆盖 repair 补证据与无证据阻断两种路径
- 已处理飞书 AI 趋势任务 plan JSON 少括号失败：
  - trace 显示多步 `web.search` 计划里 step 外层 `}` 缺失，导致 plan 阶段直接失败
  - `parsePlan()` 已增加 step 对象闭合括号修复兜底
  - 已补单测覆盖该类多步搜索计划
- 已落地 `PlanChecker / PlanNormalizer` 第一版：
  - 大模型生成的 raw plan JSON 会先经过 `CheckAndRepairPlanJSON()`
  - checker 负责 JSON 提取、常见坏 JSON 修复、schema 校验、默认字段补齐
  - runtime trace 会记录 `checker_fixed / checker_warns`，便于后续定位模型计划质量
- 已处理 `issue.md` 第 7-11 项：
  - `ResponseSanitizer` 和飞书出站渲染都会清理 `[TOOL_CALL]`、`tool`、`args` 等工具调用痕迹
  - 飞书危险命令待确认文案改为中文用户化提示，说明风险和回复方式
  - 飞书 WebSocket 显式忽略 `message_read` 与 `bot_p2p_chat_entered`，减少 not found handler 噪音
  - runtime 执行阶段不再信任模型对 safe read step 的 `requires_confirm=true`，确认边界改由工具策略决定
  - planner 提示明确项目概览优先 `project.index`、单文件摘要优先 `file.summary`
  - followup 对“拆成三条 / 步骤 / 检查项 / 可执行”等二次结构化指令增加优先级强化
- 已完成 CLI 与飞书的新一轮真实模型测试，并新增两类需要继续处理的问题：
  - 飞书最终回复会把工具调用痕迹直接回显给用户，`ResponseSanitizer` 还需要进一步覆盖飞书出站内容
  - 飞书 followup 能接上会话，但“继续上一轮 / 拆成三条检查项”这类指令的结构化输出还不够稳定
  - 飞书危险命令拦截已生效，但待确认提示文案仍偏内部化，用户体验需要再打磨
  - 飞书日志里仍有较多未处理事件噪音，后续需要做事件降噪或显式忽略
- 已处理新一轮批测发现的问题：
  - 修复复杂中文任务下模型计划 JSON 字符串内部未转义双引号导致的解析失败
  - `parsePlan()` 已增加一次本地 JSON 修复兜底，并保留标准 JSON schema 校验
  - 已新增 `internal/model/planner_test.go` 覆盖中文引号坏 JSON 计划
  - 已增强 `fresh-search`，要求搜索计划优先命中官方、一手来源、release notes、GitHub、changelog 等
  - 已增强 `source-evaluation`，要求按官方/一手、权威媒体、二手整理、时间不明区分来源质量
  - `web.search` 输出已增加来源质量提示，帮助最终总结降权二手榜单、转载页和时间戳不明来源
  - 已在 `issue.md` 标记第 5 项已处理、第 6 项部分处理
- 已处理 `issue.md` 中本轮批测暴露的前三个优先问题，并部分处理第 4 个质量判定问题：
  - followup 主链已在模型判断前接入 `internal/followup.Resolver`
  - “继续上一轮 / 继续刚才 / 接着刚才”等明确 active followup 不再因模型低置信直接 ambiguous
  - “昨天 / 上次 / 之前 / 历史”等可能指向历史任务的输入仍交给模型选择，避免误绑 active task
  - `runtime.Response` 已携带真实请求级 `TraceID`
  - `mateway test` 报告已改用 runtime 真实 trace id 加载 Trace Events
  - `mateway test` 报告新增“质量提示”，用于提示机制完成但答案可能偏浅的场景
  - 已在 `issue.md` 标记对应处理状态
- 已新增 `mateway test` 回归测试命令：
  - 会调用真实模型和现有 runtime 主链执行完整流程
  - 支持按任务名、问题、会话 key、线程 id、项目根目录等参数定制
  - 会在 `testdata/YYYY-MM-DD/` 下生成单任务单文档的 Markdown 报告
  - 报告分为问题、结果、结论、执行过程与参数、Plan、Tool Results、Trace Events，方便快速排障
- 已修复本轮整体 review 发现的安全与稳定性问题：
  - 模型计划里的 `confirmed/confirm` 参数不再被信任，危险命令和文件写入/patch 只能由用户对 pending approval 的明确回复放行
  - 用户拒绝待确认操作时会取消当前任务，并保留 `abandoned` 状态
  - Feishu/gateway 对同一个 `session_key` 的消息改为串行处理，避免并发 Load/Save 覆盖 session
  - followup 模型返回的 `target_task_id/source_task_id` 会先校验是否存在，不存在时转为澄清
  - `web.search` 现在尊重 provider 开关，无可用搜索 provider 时直接报错，不再隐式访问 DuckDuckGo
- 已新增 artifact “可直接回答”能力：
  - 在 `AgentLoop` 加载 session 后、进入 task binding / planning 前，先识别“昨天那个文档放哪了”“上次那个链接发我”这类历史产物查询
  - 命中已有 task artifacts 时直接返回文件路径或链接，不再额外走一轮 planning
  - 未命中或意图过泛时仍安全回退到原有 planning 主链
  - 已新增 `runtime.artifact_lookup` trace 事件
  - 直接回答只记录为普通对话，不创建新 task，避免污染任务历史
- 已把英文说明文档统一改成中文：
  - `README.md`
  - `docs/README.md`
- 已增强 `artifacts` 提取与历史候选压缩：
  - 会从 `web.search` 输出里提取查询词、URL、标题线索
  - 会从结果文本里补抓路径和链接类 artifact
  - followup 模型提示里的 open/history tasks 改成按当前消息相关度排序
  - task 摘要里的 artifacts 改成压缩版，不再把完整历史全量塞进 prompt
- 已完成 `TaskBindingResolver + model followup` 第一版：
  - 主链已调整为 `LoadSession -> TaskBindingResolver -> AgentLoop -> SaveSession`
  - 不再单独先判 `is_followup`
  - 已支持 `approval_reply / slot_fill / active_followup / open_task_followup / historical_continuation / new_task / ambiguous`
  - 批准与补参由代码处理，复杂 followup 交给专门模型解析
  - 已新增 `runtime.task_binding_started / runtime.task_activated / runtime.task_pending_input / runtime.task_pending_approval / runtime.task_continuation_created`
- 已完成 `project.index / file.summary` 第一版：
  - 新增 `project.index`
  - 新增 `file.summary`
  - 两者都接入现有 `tool.Registry`
  - 默认受路径安全边界和输出预算约束
  - `project.index` 可输出目录概览、扩展名分布和 sample tree
  - `file.summary` 可输出文件元信息、heading 线索和 preview
- 已完成 `FollowupResolver` 第一版：
  - 已升级成 task binding 机制
  - 已输出 `resolved_query` 并进入后续 planning / synthesis
  - 已新增 `runtime.followup_resolved` trace 事件
  - 当前已覆盖批准、补参、切回 open task、历史 continuation 和澄清
- 已完成 `SessionState / TaskState` 第一版：
  - 新增 `internal/session`
  - 默认把 session 状态保存到 `~/.mateway/run/sessions`
  - `AgentLoop` 每轮开始会加载 session，每轮结束会保存 task
  - 已保存最近 turns、`active_task_id`、task map / task order
  - 已保存 pending approval / pending questions / artifacts
  - 已新增 `runtime.session_loaded / runtime.session_saved` trace 事件
- 已完成普通任务 prompt context 第一版：
  - `planning / planning_repair / synthesis` 三个阶段统一注入
  - 注入当前日期、时区、当前请求
  - 注入基础环境摘要：操作系统、架构、shell、HOME、workspace、project root、常用命令摘要
  - 注入 `soul.md / agent.md / user.md / memory.md / tools.md`
  - 明确 `heartbeat.md` 不进入普通任务 prompt
- 已新增 `runtime.skills_selected` trace 事件：
  - 记录 planning / planning_repair / synthesis 阶段选中了哪些 skills
  - 记录 skill 来源目录、priority 和命中原因
- 已修正 `fresh-search`：
  - 不再鼓励模型擅自扩成 `2024-2025`
  - 改为结合当前日期、时区和最近 12 个月语义理解“最新”
- 已修正 `user.ask` 控制回复：
  - `input_required` 不再把前面工具过程整段返回给用户
  - 优先直接返回补问内容本身
- 已把 `soul.md / agent.md / user.md / memory.md / tools.md` 默认放进 `workspace/agents/main`
- 已把当前运行二进制重新构建并重启 gateway 服务
- 已新增 `ResponseSanitizer` 第一版：
  - 最终回复、错误回复、待确认回复统一经过出站清洗
  - 会移除常见 prompt 回声，如 `Selected skills:` / `User task:`
  - 会收敛多余空行并限制回复长度
- 已给通用 `SkillSelector` 增加基础上下文预算控制：
  - 已支持按 stage 做差异化预算
  - 已支持按 `scope` 做冲突去重
  - 仍通过 priority 排序控制保留顺序
- 已更新 `docs/开发TODO.md`：
  - `AgentLoop / ToolRegistry / SkillRegistry / SkillDiscovery / 首批 skills / SessionState / FollowupResolver` 已按当前进度改为完成或部分完成
- 已去掉 `chinese-summary` 的专用硬编码执行路径。
- 已新增通用 `SkillSelector + SkillInjector` 第一版：
  - planning 阶段可选择并注入 skills
  - synthesis 阶段可选择并注入 skills
  - 选中的 `SKILL.md` 正文会进入模型上下文
- `fresh-search / chinese-summary / source-evaluation` 已开始按元数据驱动选择：
  - `stage`
  - `when_contains`
  - `when_result_kinds`
  - `when_user_language`
- 已支持 `mateway init` 初始化 `~/.mateway` 基础目录。
- 已支持把默认 skills 随程序分发并释放到 `workspace/skills`：
  - 当前默认模板已包含 `chinese-summary`
  - 已补默认模板：`fresh-search`、`source-evaluation`
  - 不会覆盖用户已存在的 skill 文件
- 已新增基于目录的 `SkillDiscovery` 第一版：
  - 扫描 `workspace/skills`
  - 扫描 `workspace/agents/main/skills`
  - 当前可从目录加载 `chinese-summary`
  - 已验证 agent 私有 skill 会覆盖 workspace 公用 skill
- 已更新 `docs/总规划.md` 的 skills / workspace 路线：
  - `~/.mateway` 初始化时建立目录结构
  - `workspace/skills` 作为公用 skills
  - `workspace/agents/main/skills` 作为 agent 私有 skills
  - skill 目录结构按 `SKILL.md + agents/openai.yaml + references/scripts/assets` 组织
  - 已明确成品二进制分发要求：默认 skills 需要随程序分发，并在初始化时释放到 `workspace/skills`
- 已新增 `internal/skill`：
  - `skill.Registry`
  - 内置 skills 装配入口
  - 第一个内置 skill：`chinese-summary`
- 已把 skills 接入 `AgentLoop` 的结果后处理阶段：
  - 工具执行完成后可按条件对结果做轻量增强
  - 当前先用于 `web.search` 的中文总结
- 已补 `tool.NewBuiltinRegistry()`，让工具注册集有统一装配入口。
- 已开始按 P0/T0.1 重构最小内核：
  - 新增 `internal/runtime/agent_loop.go`
  - 把 runtime 主链拆成 `receive -> plan -> act -> repair -> synthesize -> reply`
  - `Runtime.Handle()` 现在只负责启动 `AgentLoop`
- 已移除飞书默认过程进度消息设计，改为优先只发送最终回复或待确认/待补问结果。
- 已根据 `docs/总规划.md` 收敛主架构方向，不再继续膨胀总纲。
- 已更新 `AGENTS.md`，明确：
  - 总纲定方向
  - TODO 定执行
  - 代码按 TODO 一项项落地
- 已新增 `docs/开发TODO.md`，按 P0-P4 拆分开发阶段，并补充目标、建议落点、交付物和验收标准。
- 已梳理当前代码目录与总纲映射，确认：
  - `internal/runtime` 已有 AgentLoop 雏形
  - `internal/gateway` 已承接 channel 编排与 session key
  - `AgentProfile` 仍待正式落地
  - `ToolRegistry` 需要补稳定装配层，`SkillRegistry` 需要尽快建立
- 新建干净 Go 项目，不继承旧 `runtimev2 / agentv2 / toolv2 / workflowv2`。
- 读取现有 `~/.mateway/config`、`~/.mateway/config/mateway.env`、MiniMax 与飞书配置。
- 接入 MiniMax Anthropic-compatible Messages API。
- 实现 JSON 计划、最多一次计划修正、工具执行、证据收集、最终总结。
- 实现工具注册表与基础工具：
  - `time.now`
  - `config.summary`
  - `web.search`
  - `file.read`
  - `file.write`
  - `file.patch`
  - `shell.run`
  - `user.ask`
- 实现危险命令识别与确认拦截。
- 实现路径限制与输出截断。
- 实现 CLI：
  - `mateway doctor`
  - `mateway ask <message>`
  - `mateway feishu`
  - `mateway gateway serve`
- 实现飞书 WebSocket、文本回复、处理中/完成/待确认 reaction。
- 已确认系统存在常驻 LaunchAgent：`com.dongping.mateway.gateway`。
- 已增加 `gateway serve` 兼容入口，可复用当前 plist 的命令形态。
- 文件相对路径默认根已调整为 `~/.mateway`，同时允许当前项目目录作为开发根。
- 已构建 `build/mateway`。
- 已通过 `launchctl kickstart -k gui/$(id -u)/com.dongping.mateway.gateway` 重启常驻进程。
- 当前常驻进程命令为：`/Users/dongping/project/mateway/build/mateway gateway serve`。
- 日志显示已连接飞书 WebSocket。
- 已补结构化 JSONL trace：`~/.mateway/trace/events-YYYY-MM-DD.jsonl`。
- 已把 `feishu` 命令标记为开发快捷入口；正式常驻入口是 `gateway serve`。
- 已收紧 channel / gateway 边界：channel 只负责收发与归一化，runtime 调用、reaction 策略和 session key 都归 `internal/gateway`。
- 已恢复 gateway 服务管理命令，但重新定义边界：
  - `gateway serve` 是真实前台服务进程
  - `gateway start/restart/stop/status` 是当前 OS 的服务管理适配层
  - macOS 管 LaunchAgent label：`com.dongping.mateway.gateway`
  - Linux 预留 user systemd unit：`mateway-gateway.service`
  - 其他 OS 暂未实现，提示用户用系统服务管理器运行 `gateway serve`
- 已新增 `scripts/restart-plist.sh --env-file ~/.mateway/config/mateway.env`：
  - 编译 `build/mateway`
  - 加载指定 env 文件
  - 通过 `launchctl kickstart -k gui/$(id -u)/com.dongping.mateway.gateway` 重启已有 LaunchAgent
  - 不生成、不安装、不修改 plist
- 已新增 trace CLI：
  - `mateway trace tail`
  - `mateway trace tail --no-follow -n 40`
  - `mateway trace show <trace_id>`
  - `--raw` 可输出原始 JSONL
- 已新增 `gateway serve` 单实例锁：
  - 锁文件：`~/.mateway/run/mateway.lock`
  - 即使用户从另一个 plist、登录项或终端启动第二个 Mateway，第二个服务进程也会拿锁失败并退出
  - 因此不再依赖 `gateway restart` 这类命令来保证唯一进程
- 已定位一次重复回复的主要风险：
  - 同一个飞书 message id 在 trace 中被处理了两轮
  - 可能原因是 WebSocket handler 等模型/工具执行太久，飞书/SDK 触发重投递
- 已调整飞书处理链路：
  - 收到消息后快速返回 ACK
  - 后台 goroutine 执行 runtime
  - `message_id` 在处理中和短时间已完成窗口内去重
  - 当前默认不发送过程进度消息，只在结束时回复一次最终结果或待确认结果
  - 飞书 `sender_type=app` 的消息会被忽略，避免 Mateway 自己的回复回环成新用户任务
- 飞书消息 session key 使用 `feishu:<thread_id>`，避免后续多 channel 串台。
- CLI 消息 session key 使用 `cli:cli`；runtime 兜底生成 `<channel>:<thread/user/id>`，避免漏传。

## 已验证

- `go test ./...` 通过。
- `go run ./cmd/mateway doctor` 通过，能读取：
  - home：`/Users/dongping/.mateway`
  - workspace：`/Users/dongping/.mateway/workspace`
  - model：`minimax / MiniMax-M2.7`
  - feishu websocket：已启用
- CLI smoke 已通过：
  - 当前时间查询
  - `pwd` 本地命令
  - 读取并总结 `README.md`
  - 危险删除命令拦截
  - 搜索 MiniMax M2.7 并总结
- `./build/mateway doctor` 通过。
- `launchctl print gui/$(id -u)/com.dongping.mateway.gateway` 显示服务 `state = running`。
- runtime 会记录 `runtime.receive / runtime.plan / runtime.tool_start / runtime.tool_done / runtime.reply` 等事件，测试后可按 `trace_id` 排查。
- trace 中已验证写入 `session_key` 字段。
- `mateway trace tail --no-follow -n 5` 可格式化查看最新事件。
- `mateway trace show <trace_id>` 可按 trace id 聚合一次请求的事件。
- `gateway` 单实例锁单测通过。
- `gateway status` 会显示单实例锁 PID 和当前 OS 服务状态。
- 飞书事件归一化已验证能读取 `sender_type`。

## 当前未完成

- 2026-05-20：已梳理下一阶段方向：
  - 根目录 README 定位为面向 GitHub 开发者的项目入口。
  - `docs/` 继续作为内部开发、规划、测试文档。
  - LICENSE 当前为 Apache-2.0，适合独立开发者希望开源采用并保留专利授权清晰度的场景；如后续做企业版，可在 Apache-2.0 开源核心之外提供商业支持、托管服务、私有 connector 或双授权模块。
  - 下一阶段优先做 Markdown-first 记忆系统，而不是先引入重型 LLM wiki / 向量 RAG。
  - 记忆系统建议以 Markdown 为可读真相源，SQLite 只作为索引和元数据层候选。
  - 企业 API / CLI 接入方向已拆成 connector package、API adapter、CLI adapter、fixed workflow 四块。
- 2026-05-20：已完成记忆沉淀与 skill 结晶第一版基础设施：
  - 新增 `workspace/memory` 统一记忆根目录，包含 `user / org / agents/<agent_id>`。
  - `mateway init` 会生成 Markdown / Obsidian-compatible memory wiki 骨架。
  - 新增 `internal/memory`，任务成功后记录 pattern、更新 counter，达到阈值后生成 `skill-candidate-*.md`。
  - 新增 `mateway memory lint`，默认只读检查 frontmatter、sources、broken wikilinks。
  - Heartbeat / scheduler 暂只落配置和设计文档，不做自动后台执行。
  - 新增 `docs/记忆系统设计.md`、`docs/定时任务与Heartbeat设计.md`、`docs/企业传统软件接入设想.md`。
- 2026-05-20：已先落地多 agent 配置契约：
  - `internal/config` 增加顶层 `model` 与 `agents` 配置结构。
  - Agent profile 可声明默认模型、fallback、角色模型、heartbeat、skill/tool allow/deny、session namespace。
  - 无 agents 配置时会合成 `main` 默认 agent，保持当前单 agent 兼容。
  - app 启动模型选择改为显式默认优先，未配置时保留 `minimax` 兼容默认，避免 `api: openai` 的本地 `mlx_lm` 自动抢占默认模型。
  - 已补模型选择单测，覆盖显式 local-mlx、隐式 minimax、未知默认模型报错。
- 2026-05-20：已补配置样例文件加载防线：
  - `models/*.sample.yaml` / `models/*.example.yaml` 不会进入真实模型列表。
  - 已补单测覆盖 sample/example 模型文件忽略。
  - 已补单测覆盖启用模型同名时报错，避免 sample 或复制文件造成同名模型隐性覆盖。
  - `doctor` 验证当前真实配置仍使用 `minimax / MiniMax-M2.7`。
- 2026-05-20：已补 `mateway init` 初始化配置生成：
  - 二进制初始化会生成 `config.yaml`、`config.sample.yaml`、`mateway.env.sample`。
  - 初始化会生成 `models/minimax.yaml`、`models/local-mlx.yaml` 及对应 sample。
  - 初始化会生成 `channels/feishu.yaml`、`channels/feishu.sample.yaml` 和说明文档。
  - 初始化保持幂等，不覆盖用户已有真实配置。
  - 已用临时 `MATEWAY_HOME` 实测 `init -> doctor` 链路通过。
- 新增观察待复测：
  - B2 followup 已强化补充指令优先级，但仍需飞书实机复测确认最终输出稳定拆成三条可执行小步骤
  - C1 安全命令和 D1 项目索引误入待确认已从确认策略和 planner 提示两侧修复，仍需真实模型回归确认
- `mateway test` 命令还可以继续增强，比如增加 trace 筛选、只输出摘要模式、或者把更多事件做成更易读的结构化段落。
- `AgentLoop` 虽已拆出独立骨架，但 step/result/event 模型还可以继续收口。
- `ToolRegistry / SkillRegistry` 已有第一版装配层，`SkillDiscovery` 也已接入目录扫描，selector 也已有 scope 去重和阶段预算，但多 skill 协同还比较基础。
- 默认 skills 的二进制内置模板与初始化释放逻辑已实现，但模板集合还不完整。
- `fresh-search / source-evaluation` 已进入通用 skill 通路，但执行效果还需要继续打磨，`project-review` 还没有落地。
- `SkillDiscovery` 已接入主链，也已有优先级、scope 冲突去重和阶段预算，但还没有更强的多 skill 协同策略。
- `AgentProfile / AgentRegistry / GatewayBinding` 已完成配置契约第一步，运行时 AgentRegistry 与 GatewayBinding 还未开始。
- 飞书实机消息测试还需要用新版本再发一条长一点的任务，确认只保留一条最终回复且过程会更新。
- 待确认/审批的恢复执行还未做持久化。
- 文件写入/patch 目前默认 preview/confirm，飞书确认后的 resume 还未完成。
- 搜索 fallback 目前是 Tavily 优先、DuckDuckGo instant answer 兜底，深度网页抽取未做。
- 没有长期记忆、多 agent、插件市场、完整 workflow。

## 下一步建议

1. 继续打磨 `SkillSelector` 的多 skill 协同策略和更细的命中规则。
2. 视 followup 效果，再补 session/task 的恢复策略、artifact 提取和历史任务压缩。
3. 继续增强 `project.index / file.summary` 的代码结构理解能力。
4. 再往后进入 `AgentProfile / AgentRegistry / GatewayBinding`。
